@using Kendo.Mvc.UI
@using KendoDP2.Areas.Organizacion.Models
@{
    ViewBag.Title = "PuestosTrabajo";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@{Html.RenderPartial("Titulo", new KendoDP2.Models.Helpers.PageTitle("PuestosTrabajo", "Administre los puestos de trabajo de la empresa"));}

<div class="portlet box green">
    <div class="portlet-title">
        <h4><i class="icon-reorder"></i>PuestosTrabajo</h4>
        <div class="tools">
	        <a href="javascript:;" class="collapse"></a>
        </div>
	</div>
    
    <div class="portlet-body">

           <div class="row-fluid">
            @(Html.Kendo().Grid<PuestoDTO>()
                .Name("PuestoGrid")
                .Columns(columns =>
                    {
                        columns.Bound(p => p.ID);
                        columns.Bound(p => p.Nombre);
                        columns.Bound(p => p.Descripcion);
                       
                        columns.Command(command => { command.Edit().Text("Editar").UpdateText("Guardar").CancelText("Cancelar");
                                                     command.Destroy().Text("Eliminar");
                                                   }).Width(100);
                        
                    }
                )
                .ToolBar(toolbar =>
                {
                    toolbar.Create().Text("Registrar");
                })
                .Editable(editable => editable
                    .Mode(GridEditMode.PopUp)
                    .TemplateName("EditorColaborador")
                    .DisplayDeleteConfirmation("¿Está seguro que desea eliminar este registro?")
                    .Window(w => { w.Title("Ingrese los datos"); w.Width(900); })

                )
                .Pageable(paper => paper.Refresh(true))
                .Filterable()
                .Sortable()
                .Scrollable(p => p.Height(330))
                .DataSource(dataSource => dataSource
                    .Ajax()
                    .PageSize(20)
                    .Model(model => model.Id(p => p.ID))
                    .Create(update => update.Action("Create", "PuestosTrabajo"))
                    .Read(read => read.Action("EditingInline_Read", "PuestosTrabajo"))
                    .Update(update => update.Action("EditingInline_Update", "PuestosTrabajo"))
                    .Destroy(update => update.Action("EditingInline_Destroy", "PuestosTrabajo"))
                )
               )
        </div>
    </div>
</div>

